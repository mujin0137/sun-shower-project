# Sun-Shower 프로젝트 기능정의서

## 1. 프로젝트 개요

### 1.1 프로젝트명
**Sun-Shower (선샤워)**

### 1.2 프로젝트 목적
여행 계획자를 위한 통합 정보 제공 플랫폼으로, 날씨 정보, 대중교통 길찾기, 여행 일정 관리 기능을 하나의 웹 애플리케이션에서 제공합니다.

### 1.3 시스템 아키텍처
- **프론트엔드**: React 19.2.0 + TypeScript + Material-UI
- **백엔드**: Node.js + Express.js 4.18.2
- **데이터베이스**: MongoDB 8.0.0
- **인증**: JWT (JSON Web Token)
- **외부 API**: OpenWeather, Kakao Maps, ODsay, Naver (예정)

### 1.4 타겟 사용자
- 여행을 계획하는 개인 사용자
- 날씨 정보와 교통 정보를 한번에 확인하고 싶은 사용자
- 체계적인 여행 일정 관리가 필요한 사용자

---

## 2. 핵심 기능

### 2.1 사용자 인증 및 관리

#### 2.1.1 회원가입
**기능 설명**
- 이메일 기반 회원가입 시스템
- 비밀번호 암호화 저장 (bcryptjs)

**구현 위치**
- 프론트엔드: `/src/components/Signup.tsx`
- 백엔드: `/backend/routes/auth.js`, `/backend/models/User.js`

**주요 기능**
- 이메일 형식 검증
- 비밀번호 강도 확인
- 중복 이메일 검사
- 회원가입 성공 시 자동 로그인

**API 엔드포인트**
```
POST /api/auth/signup
Request Body: { email: string, password: string }
Response: { token: string, user: { id, email } }
```

#### 2.1.2 로그인
**기능 설명**
- JWT 토큰 기반 인증
- localStorage에 토큰 저장

**구현 위치**
- 프론트엔드: `/src/components/Login.tsx`, `/src/context/AuthContext.tsx`
- 백엔드: `/backend/routes/auth.js`, `/backend/middleware/auth.js`

**주요 기능**
- 이메일/비밀번호 인증
- JWT 토큰 발급 (유효기간: 7일)
- 자동 로그인 (토큰 유효 시)
- 로그인 상태 전역 관리 (Context API)

**API 엔드포인트**
```
POST /api/auth/login
Request Body: { email: string, password: string }
Response: { token: string, user: { id, email } }
```

#### 2.1.3 로그아웃
**기능 설명**
- 토큰 삭제 및 세션 종료

**주요 기능**
- localStorage에서 토큰 제거
- 로그인 페이지로 리다이렉트
- 전역 인증 상태 초기화

**API 엔드포인트**
```
POST /api/auth/logout
Response: { message: "Logged out successfully" }
```

#### 2.1.4 보호된 라우트 (Protected Routes)
**기능 설명**
- 미인증 사용자의 접근 차단

**구현 위치**
- `/src/components/ProtectedRoute.tsx`

**주요 기능**
- 토큰 검증
- 미인증 시 로그인 페이지로 리다이렉트
- 보호된 페이지: Main, Weather, Transportation, Calendar, Booking, Checklist

---

### 2.2 날씨 정보 서비스

#### 2.2.1 전국 날씨 조회
**기능 설명**
- 전국 50개 주요 도시의 실시간 날씨 정보 제공

**구현 위치**
- 프론트엔드: `/src/components/Weather.tsx`
- 백엔드: `/backend/services/weatherService.js`, `/backend/routes/weather.js`

**지원 도시 (50개)**
- 수도권: 서울, 인천, 수원, 성남, 용인, 고양, 안산, 부천, 안양, 남양주, 화성, 평택, 파주, 김포
- 충청권: 청주, 천안, 아산, 대전, 세종
- 전라권: 전주, 익산, 군산, 목포, 순천, 광주, 여수
- 경상권: 부산, 대구, 울산, 창원, 김해, 양산, 거제, 진주, 포항, 구미, 경주, 안동
- 강원권: 춘천, 강릉, 원주, 속초, 동해, 태백, 삼척
- 제주권: 제주, 서귀포

**제공 정보**
- 현재 기온 (°C)
- 체감 온도
- 날씨 상태 (맑음, 흐림, 비, 눈 등)
- 습도 (%)
- 풍속 (m/s)
- 풍향
- 기압 (hPa)
- 가시거리 (km)
- 구름량 (%)
- 일출/일몰 시간

**API 엔드포인트**
```
GET /api/weather/current?city={도시명}
GET /api/weather/current?lat={위도}&lon={경도}
POST /api/weather/multiple
Request Body: { cities: string[] }
Response: { city: string, temp: number, weather: string, ... }[]
```

#### 2.2.2 시간별 날씨 예보
**기능 설명**
- 선택한 도시의 24시간 시간별 날씨 예보

**제공 정보**
- 시간대별 기온
- 날씨 상태
- 강수확률 (%)
- 풍속
- 습도

**화면 구성**
- 시간대별 카드 형식 표시
- 아이콘으로 날씨 상태 시각화
- 스크롤 가능한 가로 리스트

#### 2.2.3 일별 날씨 예보
**기능 설명**
- 5일간 일별 날씨 예보

**제공 정보**
- 날짜
- 최고 기온 / 최저 기온
- 날씨 상태
- 강수확률
- 평균 습도
- 평균 풍속

**API 엔드포인트**
```
GET /api/weather/forecast?city={도시명}
Response: { date: string, tempMax: number, tempMin: number, ... }[]
```

#### 2.2.4 도시 검색 및 상세 정보
**기능 설명**
- 50개 도시 중 특정 도시 선택 시 상세 정보 표시

**주요 기능**
- 도시 버튼 클릭 시 해당 도시 날씨 자동 로드
- 시간별/일별 예보 자동 갱신
- 모바일 환경에서는 SwipeableDrawer로 표시

**외부 API 연동**
- **OpenWeatherMap API**
  - Current Weather Data API
  - 5 Day / 3 Hour Forecast API
  - 무료 플랜: 60 calls/minute

---

### 2.3 교통 길찾기 서비스

#### 2.3.1 출발지/도착지 검색
**기능 설명**
- 장소명 또는 주소 검색을 통한 위치 지정

**구현 위치**
- 프론트엔드: `/src/components/Transportation.tsx`
- 백엔드: `/backend/services/transportationService.js`, `/backend/routes/transportation.js`

**주요 기능**
- Kakao REST API 지오코딩
- 자동완성 검색 (장소명, 주소)
- 좌표 변환 (주소 → 위도/경도)
- 최근 검색 기록 표시

**API 엔드포인트**
```
GET /api/transportation/search?query={검색어}
Response: { name: string, address: string, lat: number, lng: number }[]
```

#### 2.3.2 대중교통 길찾기
**기능 설명**
- 지하철, 버스, 기차를 활용한 최적 경로 안내

**외부 API 연동**
- **ODsay 대중교통 API**
  - 지하철 경로
  - 버스 경로
  - 지하철+버스 환승
  - KTX/SRT/ITX/무궁화호 기차

**경로 옵션**
1. **최적 경로** (OPT=0): 시간, 요금, 환승 균형
2. **최소 시간** (OPT=1): 가장 빠른 경로
3. **최소 환승** (OPT=2): 환승 최소화
4. **최소 도보** (OPT=3): 걷는 거리 최소화

**제공 정보**
- 총 소요 시간 (분)
- 총 요금 (원)
- 환승 횟수
- 총 도보 거리 (m)
- 구간별 상세 정보:
  - 노선명 (지하철 호선, 버스 번호)
  - 승차역/하차역
  - 소요 시간
  - 정류장 수
  - 환승 안내

**화면 구성**
- 경로 옵션 선택 버튼 (4가지)
- 경로 목록 표시 (여러 경로 비교 가능)
- 구간별 상세 정보 펼침/접기
- 지하철 노선 색상 표시
- 버스 번호 및 유형 표시

**API 엔드포인트**
```
GET /api/transportation/directions?origin={출발지}&destination={도착지}&mode=transit&opt={0-3}
Response: {
  totalTime: number,
  totalCost: number,
  transferCount: number,
  walkDistance: number,
  legs: [{
    mode: "subway" | "bus" | "train" | "walk",
    line: string,
    from: string,
    to: string,
    duration: number,
    stops: number
  }]
}
```

#### 2.3.3 자동차 길찾기
**기능 설명**
- 자동차 경로 안내 (예정)

**상태**: 미구현 (Naver API 키 미설정)

**예정 기능**
- Naver Directions API 또는 Kakao Mobility API 사용
- 실시간 교통 정보 반영
- 최적/최단 경로 제공
- 통행료, 예상 시간 표시

**API 엔드포인트 (예정)**
```
GET /api/transportation/directions?origin={출발지}&destination={도착지}&mode=driving
```

#### 2.3.4 도보 길찾기
**기능 설명**
- 도보 경로 안내 (간단 버전)

**현재 기능**
- 직선 거리 계산
- 예상 소요 시간 (5km/h 기준)

**향후 개선 예정**
- Kakao Mobility API 도보 경로
- 실제 보행 경로 표시
- 경로 상세 안내

**API 엔드포인트**
```
GET /api/transportation/directions?origin={출발지}&destination={도착지}&mode=walking
```

#### 2.3.5 지도 시각화
**기능 설명**
- Kakao Maps SDK를 활용한 경로 표시

**구현 기능**
- 출발지 마커 (파란색)
- 도착지 마커 (빨간색)
- 경로 Polyline (빨간색 선)
- 지도 중심 자동 조정
- 확대/축소 컨트롤

**대중교통 경로 표시**
- 경유지 마커
- 환승역 표시
- 구간별 Polyline

**반응형 디자인**
- 데스크톱: 우측 지도 패널
- 모바일: SwipeableDrawer로 지도 표시

---

### 2.4 여행 일정 관리

#### 2.4.1 캘린더 뷰
**기능 설명**
- 일/주/월 단위 캘린더로 일정 확인

**구현 위치**
- 일 단위: `/src/components/CalendarDay.tsx`
- 주 단위: `/src/components/CalendarWeek.tsx`
- 월 단위: `/src/components/CalendarMonth.tsx`
- 메인 대시보드: `/src/components/Main.tsx`

**일 단위 캘린더**
- 선택한 날짜의 시간대별 일정 표시
- 시간 슬롯: 00:00 ~ 23:00
- 일정 카드 표시 (시작/종료 시간)
- 일정 클릭 시 수정 모달 열기

**주 단위 캘린더**
- 일주일 (월~일) 일정 표시
- 요일별 일정 목록
- 주간 일정 개수 표시
- 좌우 화살표로 주 이동

**월 단위 캘린더**
- 달력 형식으로 한 달 일정 표시
- 날짜별 일정 개수 표시
- 일정 있는 날짜 강조 표시
- 날짜 클릭 시 해당 날짜 일정 표시
- 좌우 화살표로 월 이동

#### 2.4.2 일정 추가
**기능 설명**
- 새로운 여행 일정 생성

**구현 위치**
- `/src/components/ScheduleModal.tsx`

**입력 항목**
- **여행 분류**: 여행일정 1 / 여행일정 2 / 여행일정 3
- **일정명**: (필수)
- **날짜**: YYYY-MM-DD
- **시작 시간**: HH:MM
- **종료 시간**: HH:MM
- **일정 내용**: 메모
- **장소 유형**: 식당, 카페, 호텔, 쇼핑, 활동, 기차, 기타
- **장소명**
- **연락처**
- **주소**

**API 엔드포인트**
```
POST /api/schedules
Request Body: {
  tripPlan: string,
  scheduleName: string,
  scheduleDate: string,
  startTime: string,
  endTime: string,
  scheduleContent: string,
  placeType: string,
  placeName: string,
  contact: string,
  address: string
}
Response: { schedule: { _id, ...fields } }
```

#### 2.4.3 일정 조회
**기능 설명**
- 사용자의 모든 일정 또는 특정 일정 조회

**주요 기능**
- 전체 일정 목록 조회
- 날짜별 필터링
- 여행 분류별 필터링 (여행일정 1/2/3)
- 특정 ID 일정 상세 조회

**API 엔드포인트**
```
GET /api/schedules                    # 모든 일정 조회
GET /api/schedules?date={날짜}        # 특정 날짜 일정
GET /api/schedules?tripPlan={분류}    # 특정 여행 분류
GET /api/schedules/:id                # 특정 일정 조회
```

#### 2.4.4 일정 수정
**기능 설명**
- 기존 일정 정보 변경

**주요 기능**
- 일정 클릭 시 ScheduleModal 열림
- 기존 정보 자동 입력
- 모든 필드 수정 가능
- 수정 후 즉시 반영

**API 엔드포인트**
```
PUT /api/schedules/:id
Request Body: { ...수정할 필드들 }
Response: { schedule: { _id, ...fields } }
```

#### 2.4.5 일정 삭제
**기능 설명**
- 일정 삭제

**주요 기능**
- 삭제 확인 다이얼로그
- 삭제 후 캘린더 자동 갱신

**API 엔드포인트**
```
DELETE /api/schedules/:id
Response: { message: "Schedule deleted successfully" }
```

#### 2.4.6 일정 표시 (ScheduleItem)
**기능 설명**
- 일정 항목을 카드 형식으로 표시

**구현 위치**
- `/src/components/ScheduleItem.tsx`

**표시 정보**
- 장소 유형 아이콘 (식당, 카페, 호텔 등)
- 시작/종료 시간
- 일정명
- 장소명
- 주소
- 연락처
- 일정 내용

**상호작용**
- 클릭 시 수정 모달 열기
- 삭제 버튼 (휴지통 아이콘)

---

### 2.5 즐겨찾기 관리

#### 2.5.1 즐겨찾기 추가
**기능 설명**
- 자주 방문하는 장소 저장

**구현 위치**
- `/src/components/Booking.tsx`
- 백엔드: `/backend/routes/favorites.js`, `/backend/models/Favorite.js`

**저장 정보**
- 장소명
- 주소
- 좌표 (위도, 경도)
- 카테고리: home(집), work(직장), school(학교), favorite(즐겨찾기), other(기타)
- 유형: location(장소) 또는 route(경로)

**API 엔드포인트**
```
POST /api/favorites
Request Body: {
  type: "location" | "route",
  name: string,
  address: string,
  coordinates: { lat: number, lng: number },
  category: string
}
```

#### 2.5.2 즐겨찾기 조회
**기능 설명**
- 저장된 즐겨찾기 목록 확인

**API 엔드포인트**
```
GET /api/favorites
GET /api/favorites?type=location     # 장소만
GET /api/favorites?type=route        # 경로만
```

#### 2.5.3 즐겨찾기 수정/삭제
**기능 설명**
- 즐겨찾기 정보 변경 또는 삭제

**API 엔드포인트**
```
PUT /api/favorites/:id
DELETE /api/favorites/:id
```

---

### 2.6 체크리스트

#### 2.6.1 준비물 체크리스트
**기능 설명**
- 여행 준비물 체크리스트 관리

**구현 위치**
- `/src/components/Checklist.tsx`

**주요 기능**
- 항목 추가/삭제
- 체크/언체크
- 카테고리별 분류 (옷, 세면도구, 전자기기 등)
- 진행률 표시

---

### 2.7 공유 기능

#### 2.7.1 일정 공유 링크 생성
**기능 설명**
- 여행 일정을 다른 사람과 공유

**구현 위치**
- `/src/components/ShareButton.tsx`
- 백엔드: `/backend/routes/share.js`, `/backend/models/ShareLink.js`

**주요 기능**
- 공유 링크 생성 (고유 토큰)
- 링크 유효기간 설정 (기본: 7일)
- 클립보드 복사 기능
- 공유 링크 관리

**API 엔드포인트**
```
POST /api/share/generate
Request Body: { scheduleId: string, expiresIn: number }
Response: { token: string, url: string, expiresAt: Date }

GET /api/share/:token
Response: { schedule: {...} }

DELETE /api/share/:token
Response: { message: "Share link deleted" }
```

---

### 2.8 고객 지원

#### 2.8.1 고객센터
**기능 설명**
- FAQ, 문의 기능

**구현 위치**
- `/src/components/Support.tsx`

**주요 기능**
- 자주 묻는 질문 (FAQ)
- 문의하기 폼
- 공지사항

#### 2.8.2 약관 및 정책
**구현 위치**
- `/src/components/TermsOfService.tsx`: 이용약관
- `/src/components/PrivacyPolicy.tsx`: 개인정보처리방침

---

## 3. 화면 구성

### 3.1 메인 대시보드 (Main)
**경로**: `/main`

**구성 요소**
- 상단: 사용자 정보, 로그아웃 버튼
- 좌측: 네비게이션 사이드바 (Aside)
- 중앙: 월간 캘린더
- 우측: 선택한 날짜의 일정 목록

**주요 기능**
- 한눈에 이번 달 일정 확인
- 날짜 클릭 시 일정 상세 보기
- 일정 추가 버튼 (+ 아이콘)

### 3.2 날씨 페이지 (Weather)
**경로**: `/weather`

**구성 요소**
- 좌측: 네비게이션 사이드바
- 상단: 전국 50개 도시 버튼
- 중앙: 선택한 도시의 현재 날씨 (대형 카드)
- 하단: 시간별 예보 (24시간, 가로 스크롤)
- 하단: 일별 예보 (5일, 카드 그리드)

**모바일 뷰**
- 햄버거 메뉴 (Mobilebar)
- 도시 선택 Drawer
- 세로 스크롤 레이아웃

### 3.3 교통 페이지 (Transportation)
**경로**: `/transportation`

**구성 요소**
- 좌측: 네비게이션 사이드바
- 중앙 좌측: 검색 패널
  - 출발지 입력
  - 도착지 입력
  - 교통수단 탭 (대중교통/자동차/도보)
  - 경로 옵션 (최적/최소시간/최소환승/최소도보)
  - 경로 결과 목록
- 중앙 우측: Kakao 지도
  - 출발/도착 마커
  - 경로 Polyline
  - 확대/축소 컨트롤

**모바일 뷰**
- 검색 패널 (상단)
- 지도 Drawer (하단 스와이프)

### 3.4 캘린더 페이지 (Calendar)
**경로**: `/calendar`

**구성 요소**
- 좌측: 네비게이션 사이드바
- 상단: 뷰 전환 버튼 (일/주/월)
- 상단: 여행 분류 탭 (여행일정 1/2/3)
- 중앙: 캘린더 (선택한 뷰에 따라 변경)
- 우측 또는 하단: 일정 목록
- 플로팅 버튼: 일정 추가 (+ 아이콘)

### 3.5 로그인/회원가입 페이지
**경로**: `/login`, `/signup`

**구성 요소**
- 중앙 정렬 폼
- 로고 / 제목
- 입력 필드 (이메일, 비밀번호)
- 제출 버튼
- 링크 (로그인 ↔ 회원가입)

### 3.6 네비게이션
**구현 위치**
- 데스크톱: `/src/common/aside.tsx`
- 모바일: `/src/common/Mobilebar.tsx`

**메뉴 항목**
- Home (Main)
- Weather
- Transportation
- Calendar
- Booking
- Checklist
- Support
- Logout

---

## 4. 데이터 모델

### 4.1 User (사용자)
```javascript
{
  _id: ObjectId,
  email: String (unique, required),
  password: String (hashed, required),
  createdAt: Date (default: now),
  updatedAt: Date
}
```

### 4.2 Schedule (일정)
```javascript
{
  _id: ObjectId,
  userId: ObjectId (ref: User, required),
  tripPlan: String ("여행일정 1" | "여행일정 2" | "여행일정 3"),
  scheduleName: String (required),
  scheduleDate: String (YYYY-MM-DD),
  startTime: String (HH:MM),
  endTime: String (HH:MM),
  scheduleContent: String,
  placeType: String ("식당" | "카페" | "호텔" | "쇼핑" | "활동" | "기차" | "기타"),
  placeName: String,
  contact: String,
  address: String,
  createdAt: Date (default: now)
}
```

### 4.3 Favorite (즐겨찾기)
```javascript
{
  _id: ObjectId,
  userId: ObjectId (ref: User, required),
  type: String ("location" | "route"),
  name: String (required),
  address: String,
  coordinates: {
    lat: Number,
    lng: Number
  },
  category: String ("home" | "work" | "school" | "favorite" | "other"),
  // 경로인 경우
  origin: {
    name: String,
    address: String,
    coordinates: { lat: Number, lng: Number }
  },
  destination: {
    name: String,
    address: String,
    coordinates: { lat: Number, lng: Number }
  },
  createdAt: Date (default: now)
}
```

### 4.4 ShareLink (공유 링크)
```javascript
{
  _id: ObjectId,
  userId: ObjectId (ref: User, required),
  scheduleId: ObjectId (ref: Schedule, required),
  token: String (unique, required),
  expiresAt: Date (required),
  createdAt: Date (default: now)
}
```

### 4.5 SearchHistory (검색 기록)
```javascript
{
  _id: ObjectId,
  userId: ObjectId (ref: User, required),
  type: String ("weather" | "transportation"),
  query: String,
  result: Mixed,
  createdAt: Date (default: now)
}
```

---

## 5. API 명세

### 5.1 인증 API
| 메서드 | 엔드포인트 | 설명 | 인증 필요 |
|--------|-----------|------|-----------|
| POST | `/api/auth/signup` | 회원가입 | ❌ |
| POST | `/api/auth/login` | 로그인 | ❌ |
| POST | `/api/auth/logout` | 로그아웃 | ✅ |
| GET | `/api/auth/me` | 현재 사용자 정보 조회 | ✅ |

### 5.2 날씨 API
| 메서드 | 엔드포인트 | 설명 | 인증 필요 |
|--------|-----------|------|-----------|
| GET | `/api/weather/current` | 현재 날씨 (도시명 또는 좌표) | ❌ |
| GET | `/api/weather/forecast` | 5일 일별 예보 | ❌ |
| POST | `/api/weather/multiple` | 여러 도시 날씨 (최대 50개) | ❌ |

### 5.3 교통 API
| 메서드 | 엔드포인트 | 설명 | 인증 필요 |
|--------|-----------|------|-----------|
| GET | `/api/transportation/search` | 장소 검색 | ❌ |
| GET | `/api/transportation/directions` | 경로 검색 (주소 기반) | ❌ |
| GET | `/api/transportation/directions/coordinates` | 경로 검색 (좌표 기반) | ❌ |

### 5.4 일정 API
| 메서드 | 엔드포인트 | 설명 | 인증 필요 |
|--------|-----------|------|-----------|
| GET | `/api/schedules` | 모든 일정 조회 | ✅ |
| GET | `/api/schedules/:id` | 특정 일정 조회 | ✅ |
| POST | `/api/schedules` | 일정 생성 | ✅ |
| PUT | `/api/schedules/:id` | 일정 수정 | ✅ |
| DELETE | `/api/schedules/:id` | 일정 삭제 | ✅ |

### 5.5 즐겨찾기 API
| 메서드 | 엔드포인트 | 설명 | 인증 필요 |
|--------|-----------|------|-----------|
| GET | `/api/favorites` | 모든 즐겨찾기 조회 | ✅ |
| POST | `/api/favorites` | 즐겨찾기 추가 | ✅ |
| PUT | `/api/favorites/:id` | 즐겨찾기 수정 | ✅ |
| DELETE | `/api/favorites/:id` | 즐겨찾기 삭제 | ✅ |

### 5.6 공유 API
| 메서드 | 엔드포인트 | 설명 | 인증 필요 |
|--------|-----------|------|-----------|
| POST | `/api/share/generate` | 공유 링크 생성 | ✅ |
| GET | `/api/share/:token` | 공유된 일정 조회 | ❌ |
| DELETE | `/api/share/:token` | 공유 취소 | ✅ |

---

## 6. 외부 API 연동

### 6.1 OpenWeatherMap API
**용도**: 날씨 정보 조회

**API 키**: `OPENWEATHER_API_KEY`

**사용 API**
- Current Weather Data API
- 5 Day / 3 Hour Forecast API
- One Call API (선택)

**요청 제한**: 무료 플랜 60 calls/minute

**예제 요청**
```
https://api.openweathermap.org/data/2.5/weather?q=Seoul&appid={API_KEY}&units=metric&lang=kr
https://api.openweathermap.org/data/2.5/forecast?q=Seoul&appid={API_KEY}&units=metric&lang=kr
```

### 6.2 Kakao Maps API
**용도**: 지도 표시, 장소 검색, 지오코딩

**API 키**: `KAKAO_REST_API_KEY`, JavaScript 키 (프론트엔드)

**사용 API**
- REST API
  - 주소 검색 (Address)
  - 좌표-주소 변환 (Geocoding)
  - 키워드 장소 검색 (Local)
- Maps SDK
  - 지도 표시
  - 마커
  - Polyline

**문서**: https://developers.kakao.com/docs/latest/ko/local/dev-guide

**예제 요청**
```
https://dapi.kakao.com/v2/local/search/keyword.json?query=강남역
https://dapi.kakao.com/v2/local/search/address.json?query=서울특별시
```

### 6.3 ODsay API
**용도**: 대중교통 경로 검색

**API 키**: `ODSAY_API_KEY`

**사용 API**
- 대중교통 경로 검색 API
- 지하철 정보 API
- 버스 정보 API

**지원 교통수단**
- 지하철
- 버스
- 지하철+버스 환승
- KTX, SRT, ITX, 무궁화호

**문서**: https://lab.odsay.com/

**예제 요청**
```
https://api.odsay.com/v1/api/searchPubTransPathT?SX=126.926493&SY=37.656425&EX=127.126936&EY=37.396815&apiKey={API_KEY}&OPT=0
```

### 6.4 Naver Directions API (예정)
**용도**: 자동차 경로 검색

**API 키**: `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`

**상태**: 미설정 (환경변수만 존재)

**문서**: https://api.ncloud-docs.com/docs/ai-naver-mapsdirections15

---

## 7. 보안 및 인증

### 7.1 JWT 인증
**토큰 구성**
```javascript
{
  userId: ObjectId,
  email: String,
  iat: Number,  // 발급 시간
  exp: Number   // 만료 시간 (7일)
}
```

**토큰 저장**: localStorage

**인증 흐름**
1. 로그인 요청 (`/api/auth/login`)
2. 서버에서 JWT 발급
3. 클라이언트 localStorage에 저장
4. 이후 요청 시 Authorization 헤더에 포함
5. 서버에서 미들웨어로 검증 (`/backend/middleware/auth.js`)

### 7.2 비밀번호 암호화
- **라이브러리**: bcryptjs
- **Salt Rounds**: 10
- **해싱 알고리즘**: bcrypt

### 7.3 CORS 설정
```javascript
app.use(cors({
  origin: '*',  // 개발 환경 (프로덕션에서는 특정 도메인으로 제한)
  credentials: true
}));
```

### 7.4 환경 변수 보안
- `.env` 파일로 API 키 관리
- `.gitignore`에 `.env` 포함
- 민감 정보 노출 방지

---

## 8. 기술적 요구사항

### 8.1 프론트엔드
- **Node.js**: 14.x 이상
- **npm**: 6.x 이상
- **브라우저**: 최신 Chrome, Firefox, Safari, Edge

### 8.2 백엔드
- **Node.js**: 14.x 이상
- **MongoDB**: 4.x 이상 (로컬) 또는 MongoDB Atlas

### 8.3 개발 환경
- **OS**: Windows, macOS, Linux
- **IDE**: VS Code 권장
- **Git**: 형상 관리

---

## 9. 배포 및 운영

### 9.1 개발 환경 실행
```bash
# 프론트엔드
npm start           # http://localhost:3000

# 백엔드
cd backend
npm run dev         # http://localhost:5001
```

### 9.2 프로덕션 빌드
```bash
# 프론트엔드
npm run build       # build/ 폴더 생성

# 백엔드
npm start           # 프로덕션 모드
```

### 9.3 환경 변수 설정
**필수 환경 변수**
```env
# MongoDB
MONGODB_URI=mongodb://localhost:27017/sun-shower

# 날씨 API
OPENWEATHER_API_KEY=your_key_here

# 카카오 REST API
KAKAO_REST_API_KEY=your_key_here

# ODsay 대중교통 API
ODSAY_API_KEY=your_key_here

# 서버
PORT=5001
JWT_SECRET=your_secret_key_here
```

**선택적 환경 변수**
```env
# Naver Directions API (미구현)
NAVER_CLIENT_ID=your_id_here
NAVER_CLIENT_SECRET=your_secret_here
```

### 9.4 MongoDB 설정
**로컬 MongoDB**
```bash
# MongoDB 설치 (macOS)
brew install mongodb-community

# MongoDB 실행
brew services start mongodb-community

# 연결 확인
mongo
```

**MongoDB Atlas (클라우드)**
1. https://www.mongodb.com/cloud/atlas 회원가입
2. 무료 클러스터 생성
3. 데이터베이스 사용자 생성
4. IP 화이트리스트 설정
5. 연결 문자열 복사 → `.env` 파일에 입력

---

## 10. 향후 개선 계획

### 10.1 단기 계획 (1~2개월)
- [ ] Naver Directions API 연동 (자동차 경로)
- [ ] Kakao Mobility API 연동 (자동차/도보 경로)
- [ ] 실시간 교통 정보 표시
- [ ] 날씨 알림 기능 (Push Notification)
- [ ] 경로 즐겨찾기 기능 강화

### 10.2 중기 계획 (3~6개월)
- [ ] 모바일 앱 개발 (React Native)
- [ ] 소셜 로그인 (구글, 카카오, 네이버)
- [ ] 일정 공유 기능 개선 (실시간 협업)
- [ ] 날씨 기반 여행지 추천
- [ ] 예산 관리 기능
- [ ] 여행 후기 및 사진 공유

### 10.3 장기 계획 (6개월 이상)
- [ ] AI 기반 여행 경로 최적화
- [ ] 숙박 예약 기능 연동
- [ ] 항공권 검색 기능
- [ ] 다국어 지원 (영어, 일본어, 중국어)
- [ ] 해외 여행 지원 (글로벌 날씨/교통 API)
- [ ] 프리미엄 기능 (유료 구독)

### 10.4 기술적 개선
- [ ] Redis 캐싱 추가 (API 응답 속도 향상)
- [ ] API Rate Limiting (보안 강화)
- [ ] 로깅 및 모니터링 시스템 (Sentry, Winston)
- [ ] 테스트 커버리지 확대 (Jest, Cypress)
- [ ] CI/CD 파이프라인 구축 (GitHub Actions)
- [ ] Docker 컨테이너화
- [ ] Kubernetes 배포

---

## 11. 문의 및 지원

### 11.1 문서
- **프로젝트 README**: `/README.md`
- **백엔드 가이드**: `/backend/README.md`
- **API 기능 정리**: `/API_기능정리.md`

### 11.2 개발팀
- GitHub Repository: (링크 추가)
- 이슈 트래커: GitHub Issues
- 이메일: (연락처 추가)

---

## 12. 라이선스
**MIT License** (또는 프로젝트에 맞는 라이선스 선택)

---

## 부록

### A. 지원 도시 목록 (50개)
**수도권 (14개)**
서울, 인천, 수원, 성남, 용인, 고양, 안산, 부천, 안양, 남양주, 화성, 평택, 파주, 김포

**충청권 (5개)**
청주, 천안, 아산, 대전, 세종

**전라권 (6개)**
전주, 익산, 군산, 목포, 순천, 광주, 여수

**경상권 (14개)**
부산, 대구, 울산, 창원, 김해, 양산, 거제, 진주, 포항, 구미, 경주, 안동

**강원권 (7개)**
춘천, 강릉, 원주, 속초, 동해, 태백, 삼척

**제주권 (2개)**
제주, 서귀포

### B. 장소 유형 목록
- 식당
- 카페
- 호텔
- 쇼핑
- 활동 (관광지, 박물관, 테마파크 등)
- 기차 (KTX, SRT 등)
- 기타

### C. 교통 경로 옵션
- **OPT=0**: 최적 경로 (시간, 요금, 환승 균형)
- **OPT=1**: 최소 시간 (가장 빠른 경로)
- **OPT=2**: 최소 환승 (환승 최소화)
- **OPT=3**: 최소 도보 (걷는 거리 최소화)

### D. 날씨 상태 아이콘
- 맑음: ☀️
- 구름 조금: 🌤️
- 흐림: ☁️
- 비: 🌧️
- 눈: ❄️
- 천둥번개: ⛈️
- 안개: 🌫️

---

**작성일**: 2025-11-19
**버전**: 1.0
**문서 상태**: 최종
